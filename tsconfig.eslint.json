{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "module": "preserve",
    "moduleResolution": "bundler"
  },
  "include": ["lib/**/*.ts", "vitest.config.ts", "scripts/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
